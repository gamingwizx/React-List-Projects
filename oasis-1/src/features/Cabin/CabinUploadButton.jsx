import styled from "styled-components"

const CabinUploadButton = styled.label`
    background-color: var(--bg-indigo-500);
    color: white;
    padding: calc(var(--spacing) / 3) calc(var(--spacing));
    border-radius: var(--border-radius);
    text-align: center;
    white-space: nowrap;
`

export default CabinUploadButton;